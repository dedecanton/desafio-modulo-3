import styled from "styled-components";

export const Header = styled.header`
  background: #fff;
  box-shadow: 0 1px 6px rgb(74, 189, 172);
  padding: .5rem;
  position: fixed;
  width: 100%;
  left: 0;
  top: 0;
  z-index: 9999;
`;

export const HeaderLogo = styled.h1`
  color: #4abdac;
  font-size: 1.5rem;
  font-weight: bold;
`;

